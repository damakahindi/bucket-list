// import _ from 'lodash'
// let initialState = {
//   user: []
// }

export default (state = [], action) => {
  // let newState = _.merge({}, state)
  switch (action.type) {
    default:
      return state;
  }
};
